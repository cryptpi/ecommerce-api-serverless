# ─────────────────────────────────────────────────────────────────────────────
# Variables d'environnement — copiez ce fichier vers .env et remplissez.
#
#   cp .env.example .env
#
# Ce fichier (.env.example) est versionné dans Git.
# Le fichier .env contient vos secrets — il est dans .gitignore.
# ─────────────────────────────────────────────────────────────────────────────

MONGODB_URL=mongodb+srv://api_user:VOTRE_MOT_DE_PASSE@ecommerce-workshop.xxxxx.mongodb.net/ecommerce?retryWrites=true&w=majority
